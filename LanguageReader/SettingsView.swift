import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("Settings coming next")
                .foregroundStyle(.secondary)
                .navigationTitle("Settings")
        }
    }
}
