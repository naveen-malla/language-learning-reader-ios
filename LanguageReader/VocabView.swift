import SwiftUI

struct VocabView: View {
    var body: some View {
        NavigationStack {
            Text("Vocab list coming next")
                .foregroundStyle(.secondary)
                .navigationTitle("Vocab")
        }
    }
}

#Preview {
    VocabView()
}
